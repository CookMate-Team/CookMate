spring:
  cloud:
    discovery:
      enabled: false
    config:
      enabled: false
    loadbalancer:
      ribbon:
        enabled: false
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9999/realms/test

server:
  port: 0

management:
  endpoints:
    web:
      exposure:
        include: health

eureka:
  client:
    enabled: false
